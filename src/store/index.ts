import { createStore } from 'vuex'
import address from './address.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    address
  }
})
